# This workflow will build a golang project and run comprehensive tests
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Go

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

jobs:

  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.24'

    - name: Download dependencies
      run: go mod download

    - name: Build
      run: go build -v ./...

    - name: Run unit tests
      run: |
        cd pkg/snowflake-cache
        go test -v -race -cover -coverprofile=coverage.out

    - name: Upload coverage to artifacts
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: coverage-report
        path: pkg/snowflake-cache/coverage.out

  # Integration tests require Snowflake credentials
  # Uncomment and configure secrets if you want to run integration tests in CI
  # integration-tests:
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   env:
  #     SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
  #     SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
  #     SNOWFLAKE_PRIVATE_KEY: ${{ secrets.SNOWFLAKE_PRIVATE_KEY }}
  #     SNOWFLAKE_DATABASE: ${{ secrets.SNOWFLAKE_DATABASE }}
  #     SNOWFLAKE_SCHEMA: ${{ secrets.SNOWFLAKE_SCHEMA }}
  #   steps:
  #   - uses: actions/checkout@v4
  #   - uses: actions/setup-go@v4
  #     with:
  #       go-version: '1.24'
  #   - name: Run integration tests
  #     run: |
  #       cd integration-tests
  #       ./run_all_tests.sh
